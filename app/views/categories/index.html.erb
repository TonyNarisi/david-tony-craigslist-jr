<h3>Categories</h3>
<ul class="categories">
  <% @categories.each do |categ| %>
    <li><%= link_to "#{categ.name}", category_path(categ) %></li>
  <% end %>
</ul>

<div id="link">
<%= link_to "Make New Category", new_category_path, class: "button black-text" %>
</div>
